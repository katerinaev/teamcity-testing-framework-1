name: Run tests

on:
  workflow_call:
    inputs:
      package:
        description: api | ui
        required: true
        type: string

jobs:
  validate-checkstyle:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'zulu'

      - name: Give execute permission for mvnw
        run: chmod +x ./mvnw

      - name: Validate Checkstyle
        run: ./mvnw validate

  run-tests:
    needs: validate-checkstyle
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Teamcity Setup
        uses: ./.github/actions/teamcity-setup

      - name: Run tests
        continue-on-error: true
        # Указываем группу, чтобы не запустить тесты, связанные с сетапом Teamcity
        run: ./mvnw test -Dtest='com.example.teamcity.${{ inputs.package }}.**' -Dgroups=Regression

      # 🕓 Добавляем ожидание готовности TeamCity перед Swagger Coverage
      - name: Wait for TeamCity to start
        if: ${{ inputs.package == 'api' }}
        shell: bash
        run: |
          echo "⏳ Waiting for TeamCity to become available..."
          for i in {1..30}; do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://$HOST:8111/app/rest/swagger.json || true)
             if [ "$STATUS" == "200" ]; then
               echo "✅ TeamCity Swagger API is ready!"
               break
             fi
             echo "Still waiting... ($i/30) HTTP $STATUS"
             sleep 10
          done

      # Создаем репорт с покрытием API тестов, основанном на Swagger
      # Документация: https://github.com/viclovsky/swagger-coverage
      # Обязательно добавить фильтр SwaggerCoverageRestAssured в RestAssured# Сохраняем Swagger Report в артефакты GitHub Actions
      - name: Run Swagger Coverage
        continue-on-error: true
        if: ${{ inputs.package == 'api' }}
        run: |
          chmod +x .swagger-coverage-commandline/bin/swagger-coverage-commandline
                    .swagger-coverage-commandline/bin/swagger-coverage-commandline \
                      -s http://$HOST:8111/app/rest/swagger.json \
                      -i target/swagger-coverage-output
      # Сохраняем Swagger Report в артефакты GitHub Actions
      - name: Save Swagger Coverage
        uses: actions/upload-artifact@v4
        if: ${{ inputs.package == 'api' }}
        with:
          name: swagger-coverage
          path: |
            swagger-coverage-report.html
            swagger-coverage-results.json
